name: test
on:
  push:
    branches:
      - main
jobs:
  sonarcloud:
    name: SonarCloud
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: npm run install
      - name: Test and coverage
        run: npm run test-coverage

